import React from "react"

//Needs to recieve an array of available pages
const Navbar = () =>{
    return(
        <nav className="nav-wrapper">
            <div className="container">
                <a className="brand-logo">Warehouse</a>
            </div>
        </nav>
    )
}